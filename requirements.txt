# ---------------------------------------------
# 📊 DATA PROCESSING & ANALYSIS
# ---------------------------------------------
pandas==2.3.3          # Data manipulation and analysis — core for working with tabular data
numpy==2.3.4           # Numerical operations, arrays, and matrix support (used by almost all ML libraries)

# ---------------------------------------------
# 🧠 MACHINE LEARNING
# ---------------------------------------------
scikit-learn==1.7.2    # Classic ML models (regression, classification, clustering), preprocessing, model evaluation
xgboost==3.1.1         # Extreme Gradient Boosting - high-performance ML algorithm

# ---------------------------------------------
# 📈 VISUALIZATION
# ---------------------------------------------
matplotlib==3.10.7     # Base library for plotting graphs, charts, and visualizations
seaborn==0.13.2        # High-level interface for drawing attractive and informative statistical graphics (built on matplotlib)

# ---------------------------------------------
# 📦 EXPERIMENT TRACKING & MODEL MANAGEMENT
# ---------------------------------------------
mlflow==3.5.1          # End-to-end ML lifecycle tracking, including experiment tracking, model packaging, and deployment

# ---------------------------------------------
# ✅ TESTING
# ---------------------------------------------
pytest>=7.3.1          # Python testing framework — great for writing unit tests for ML pipelines, data validation, etc.

# ---------------------------------------------
# ⚡ API DEVELOPMENT (FOR MODEL SERVING)
# ---------------------------------------------
fastapi==0.120.0       # Lightweight, high-performance web framework for serving ML models via REST APIs
uvicorn==0.38.0        # ASGI server for running FastAPI apps — lightweight and fast

# ---------------------------------------------
# 🔧 UTILITIES & MISC
# ---------------------------------------------
pyyaml>=6.0.3          # YAML parser for configuration files
joblib>=1.5.2          # Efficient serialization for scikit-learn models
python-dotenv>=1.2.1   # Load environment variables from .env files

# ---------------------------------------------
# 📓 JUPYTER NOTEBOOK SUPPORT
# ---------------------------------------------
ipykernel>=7.0.1       # Jupyter kernel for Python
jupyter>=1.0.0         # Jupyter notebook environment